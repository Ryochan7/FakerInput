# TODO

* Send empty report when client app fails to send a disconnect request
* Enable multiple absolute mouse pointers in some way (IPC, control method). Needed for 2 player lightgun game support
* Make sure tracing works
* Use unique GUID for interface
* Update project to build on Visual Studio 2026
